{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recipe creation</h1>

    <div class="main_form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" />
        {{ form_end(form) }}
    </div>

{% endblock %}
